package pl.coderslab.seleniucourse.pageobject;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

import static pl.coderslab.seleniucourse.Tools.fillInput;

/*public class LogOnExistingAccount {
    private WebDriver driver;

    public LogOnExistingAccount(WebDriver driver) {
        this.driver = driver;
    }
    public void clickLogOn() {
        WebElement emailInput = driver.findElement(By.id("SubmitLogin"));
        emailInput.sendKeys("haha@haha.pl");

        WebElement passwordInput = driver.findElement(By.id("passwd"));
        emailInput.sendKeys("haslo123");

        WebElement logOnButton = driver.findElement(By.cssSelector("icon-lock.left"));
        logOnButton.click();
    }
}

*/